apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: my-ingress
  annotations:
    kubernetes.io/ingress.class: "nginx"
    nginx.ingress.kubernetes.io/rewrite-target: /$2
spec:
  rules:
    - host: 35.239.153.130.xip.io
      http:
        paths:
          - path: /ad(/|$)(.*)
            backend:
              serviceName: ad-ms
              servicePort: 8080